{
  "name": "barrel-beat",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "rm -rf public/build && tsc -p tsconfig.json",
    "serve": "python3 -m http.server 8091 --directory public"
  },
  "dependencies": {
    "howler": "^2.2.4",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
